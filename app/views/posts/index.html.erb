<div class="main">
  <p>お気に入りスポットを記録しよう！</p>

  <% if user_signed_in? %>
    <div>
      <%= link_to "新規投稿", new_post_path %>
    </div>
  <% end %>
  <h2>みんなのお気に入りスポット一覧</h2>
  <div>
    <% @posts.each do |post| %>
      <%= render partial: "posts/post", locals: { post: post } %>
    <% end %>
  </div>
</div>