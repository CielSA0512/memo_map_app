<div class="main">
  <div class="container">
    <h2>アカウント詳細</h2>
    <div class="account_info">
        <table class="account_status">
          <tr>
            <th class="">名前：</th>
            <td class=""><%= @user.nickname %></td>
          </tr>
          <tr>
            <th class="">プロフィール：</th>
            <td class=""><%= @user.profile %></td>
          </tr>
          <tr>
            <th class="">趣味：</th>
            <td class=""><%= @user.hobby %></td>
          </tr>
        </table>
      <% if user_signed_in? && current_user.id == @user.id %>
        <div class="account">
          <%= link_to "アカウント編集", edit_user_path(@user.id), class:"account_edit btn" %>
        </div>
      <% end %>
    </div>

    <div class="center-container">
      <div class="center-main-container">
        <div class="center-title"><%= @user.nickname %>さんのスポット記録一覧</div>
          <div class="center-posts">
            <% @posts.each do |post| %>
              <div class="center-post">
                <%= render partial: "posts/post", locals: { post: post } %>
              </div>
            <% end %>
          </div>
      </div>
    </div>
  </div>

  </div>
</div>